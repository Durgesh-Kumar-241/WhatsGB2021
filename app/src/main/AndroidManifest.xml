<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dktechhub.mnnit.ee.whatsappweb">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatsappWeb">
        <activity android:name=".InstagramActivity"></activity>
        <activity android:name=".FacebookActivity"
            />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".DirectChatActivity"
            android:screenOrientation="userPortrait"
            android:label="Direct Chat"/>
        <activity
            android:name=".MainActivityNew"
            android:screenOrientation="userPortrait"
            android:label="@string/title_activity_main_new">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainFragment"
            android:label="Whatsapp web" />
        <activity
            android:name=".SavedStatusActivity"
            android:label="Saved status" />
        <activity
            android:name=".StatusSaverActivity"
            android:label="Recent status" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>